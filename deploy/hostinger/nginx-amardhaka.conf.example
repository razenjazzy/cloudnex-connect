# TLS is typically certbot-managed. Merge these location blocks into the
# amardhaka.io server{} on the VPS. Do not add this file to other vhosts.
#
# Prefixes are app-aware (PUBLIC_ADMIN_BASE / PUBLIC_DEMO_BASE on the process).
# Longer Admin prefix must be matched first (^~ /cloudnex-admin/test).
# HMAC webhooks stay on the primary process (8080). Do not dual-bind the same
# LINE OA to 8081 unless the sibling has its own channel credentials.

location ^~ /cloudnex-admin/test {
    proxy_pass http://127.0.0.1:8081;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
}

location /cloudnex-admin/ {
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
}

location /cloudnex-connect/demo {
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
}

location /webhook {
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Line-Signature $http_x_line_signature;
    proxy_read_timeout 60s;
}

location = /admin {
    return 301 /cloudnex-admin/;
}
location /admin/ {
    rewrite ^/admin/(.*)$ /cloudnex-admin/$1 permanent;
}
location = /demo {
    return 301 /cloudnex-connect/demo;
}
